{
  "buildCommand": "pnpm --filter @workspace/flowpay run build",
  "outputDirectory": "artifacts/flowpay/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
