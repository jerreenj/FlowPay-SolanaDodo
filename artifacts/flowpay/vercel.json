{
  "buildCommand": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/flowpay run build",
  "outputDirectory": "artifacts/flowpay/dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
